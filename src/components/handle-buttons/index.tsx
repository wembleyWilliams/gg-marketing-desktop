import React from "react";
import {Paper} from "@mui/material";
import handleGenerator from "../../utils/handleGenerator";

const handleButtons = (businessHandles: any ) => {
  return
}

export default handleButtons